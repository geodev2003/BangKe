name: Deploy to Server

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull & rebuild
        shell: powershell
        run: |
          Set-Location "D:\Code\final"
          git pull origin main
          docker-compose up -d --build
          Write-Host "Deploy completed!"